FROM node:22-alpine
WORKDIR /app
COPY package*.json tsconfig.server.json ./
RUN npm ci
COPY server/ ./server/
RUN npx esbuild server/index.ts --bundle --platform=node --format=esm --outfile=dist-server/index.js --external:ws
EXPOSE 8080
CMD ["node", "dist-server/index.js"]
